let obj = {
    name:"karthik",
    cash:100000,
    from:"hyd",
    to:"chennai",
    by:"car",
    on:"monday"
};
console.log(`${obj.name} has travelled from ${obj.from} to ${obj.to} by a ${obj.by} on ${obj.on}.`)