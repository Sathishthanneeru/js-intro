
let username=prompt("enter ur good name")
let domain=prompt("enter the role")
let obj={
    studname:username,
    company:"karur vysya bank",
    role:domain,
    package:"13",
    joindate:"oct 03"
}

alert(`heartly congrulation to the ${obj.studname}.she got placed in ${obj.company} , the role is ${obj.role} with ${obj.package} LPA package and joing on ${obj.joindate}`)


